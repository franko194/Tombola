{
  "scripts": {
    "install:frontend": "cd frontend && npm install",
    "backend": "cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload",
    "frontend": "cd frontend && npm run dev",
    "frontend:preview": "cd frontend && npm run build && npm run preview -- --host 0.0.0.0 --port 5173",
    "build": "cd frontend && npm run build",
    "test:backend": "cd backend && python -m pytest tests -v"
  }
}
